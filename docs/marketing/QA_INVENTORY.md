# QA inventory

Claims: landing curta e mobile-first; CTA no primeiro viewport; fotografia/intenção dinâmica; links reais sem JS; tracking não bloqueia saída; UTMs/IDs consentidos; nenhum purchase; canonical/noindex limpos; sem overflow/recortes.

Controles: seletor de iFood/99Food/direto; cards de item; CTA hero/final/sticky; preferências de cookies; parâmetro `intent`; parâmetro `experiment`; links de privacidade.

Estados: hub, plataforma padrão, cada intenção, antes/depois do consentimento, retorno, JavaScript desabilitado, tracking API falhando, viewport 360/390/430/768/1440.

Exploração: query desconhecida deve cair no default; localStorage/API indisponíveis não podem quebrar CTA; link de marketplace não recebe click IDs; link Expresso preserva atribuição permitida.
