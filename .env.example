# Public identifiers (safe to expose only when the corresponding feature is configured)
VITE_TRACKING_DEBUG=false
TRACKING_DEBUG=false
META_PIXEL_ID=
GA4_MEASUREMENT_ID=
GOOGLE_ADS_ID=
GOOGLE_ADS_CONVERSION_LABEL=
GTM_WEB_ID=GTM-56F5TM96
GTM_SERVER_URL=

# Server-only secrets: configure with Cloudflare secrets, never in frontend code
META_CAPI_TOKEN=
GA4_API_SECRET=

# Optional server-side tagging flags
ENABLE_META_CAPI=false
ENABLE_GA4_MEASUREMENT_PROTOCOL=false
ENABLE_SERVER_GTM=false
